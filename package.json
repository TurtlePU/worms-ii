{
  "name": "worms-ii",
  "version": "0.0.0",
  "description": "Worms II in browser. Again",
  "scripts": {
    "build-server": "rimraf server/build && parcel build server/src/server.js -t node -d server/build --no-source-maps",
    "build-client": "rimraf client/build && parcel build client/src/index.html -d client/build --no-source-maps",
    "build": "npm run build-server && npm run build-client",
    "start": "node server/build/server.js",
    "watch-server": "rimraf server/dist && parcel server/src/server.js -t node -d server/dist",
    "watch-client": "rimraf client/dist && parcel watch client/src/index.html -d client/dist",
    "dev-server": "cross-env DEV_SERVER=true nodemon server/dist/server.js"
  },
  "author": "P.U. Turtle",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/TurtlePU/worms-ii.git"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "nodemon": "^1.19.1",
    "parcel-bundler": "^1.12.3",
    "rimraf": "^2.6.3"
  }
}
